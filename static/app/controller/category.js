angular.module('app.controller.category', [])
    .controller('categoryController', function ($scope, $rootScope, $filter, $timeout, $http, moment, storage, CONFIG) {
        $scope.model = {

        }

        $scope.run = {
            init:() => {
                // 카테고리 목록 조회 (로컬, 서버 적용 된버전 mix)
            }
        }
    })
